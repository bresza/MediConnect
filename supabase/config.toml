# Configuracao das Edge Functions do projeto.
# A funcao ai-chat valida o Bearer JWT com GET /auth/v1/user (usuario
# logado, nao a anon key). Desligamos `verify_jwt` no gateway so para
# o preflight CORS (OPTIONS sem Authorization) nao ser rejeitado.

[functions.ai-chat]
verify_jwt = false
